import { Button } from "@mui/material";

const AddSectionButton = () => {
  return (
    <Button color="inherit" variant="contained" sx={{ mt: 2, mx: 1 }}>
      ADD SECTION
    </Button>
  );
};

export default AddSectionButton;
